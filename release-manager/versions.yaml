# this file is used to extract the versions of the application that are to be released
# the addition of new versions is done only by dev team
# this versions definition file also defines the target environments for the application
# as well as the environment precedence

application:
  name: Rlease Manager
  type: k8s
  envs: [staging]

  versions:
    0.8.9:
      backend:
        image: aixpand/deploy-app:0.8.9
      summary: "fix gitaction watch & logs"
    0.8.8:
      backend:
        image: aixpand/deploy-app:0.8.8
      summary: "fix gitaction watch & logs"
    0.8.7:
      backend:
        image: aixpand/deploy-app:0.8.7
      summary: "added gitaction watch & logs"
    0.8.6:
      backend:
        image: aixpand/deploy-app:0.8.6
      summary: "fix typo inn variable name"
    0.8.5:
      backend:
        image: aixpand/deploy-app:0.8.5
      summary: "fix UI error I.message.forEach"
    0.8.4:
      backend:
        image: aixpand/deploy-app:0.8.4
      summary: "fix log with color"
    0.8.3:
      backend:
        image: aixpand/deploy-app:0.8.3
      summary: "(BROKEN) logging & requirements"
    0.8.2:
      backend:
        image: aixpand/deploy-app:0.8.2
      summary: "(BROKEN) added more logging"
    0.8.1:
      backend:
        image: aixpand/deploy-app:0.8.1
      summary: "(BROKEN) added more logging"
    0.8.0:
      backend:
        image: aixpand/deploy-app:0.8.0
      summary: "(BROKEN) feat: show errors in modal window"
    0.7.4:
      backend:
        image: aixpand/deploy-app:0.7.4
      summary: "chore: version bumped for testing"
    0.7.3:
      backend:
        image: aixpand/deploy-app:0.7.3
      summary: "feat: enable versioned images in docker hub"

